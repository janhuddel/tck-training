{
  "name": "tck-training",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "node scripts/clean.js"
  },
  "devDependencies": {
    "@nx/angular": "^20.6.4",
    "@nx/js": "^20.6.4",
    "@nx/workspace": "^20.6.4",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.11.18",
    "nx": "^20.6.4",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
